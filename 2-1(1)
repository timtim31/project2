#include <stdio.h>
#include <stdlib.h>

double fun_scan();
int max(int a, int b, int c, int maximum, int average, int minimum);
int min(int a, int b, int c, int maximum, int average, int minimum);
int main();
int main()
{
    int a, b, c, z, q;
    int maximum = 0;
    int average = 0;
    int minimum = 0;
    a = fun_scan();
    b = fun_scan();
    c = fun_scan();
    z = max(a,b,c,maximum, average, minimum);
    q = min(a,b,c,maximum, average, minimum);
    
    printf("%d\n", z);
    printf("%d\n", q);
    return 0;
}

int max(int a, int b, int c, int maximum, int average, int minimum)
{
    if ((a>b)&&(a>c))
    {
        maximum=a;
        if (b<c)
        {
            minimum=b;
            average=c;
            printf(" %d\n%d\n%d\n", maximum, average, minimum);
            abort();
        }
        else
        {
            minimum=c;
            average=b;
            printf(" %d\n%d\n%d\n", maximum, average, minimum);
            abort();
        }
    }
    if ((b>a)&&(b>c))
    {
        maximum=b;
        if (a<c)
        {
            minimum=a;
            average=c;
            printf(" %d\n%d\n%d\n", maximum, average, minimum);
            abort();
        }
        else
        {
            minimum=c;
            average=a;
            printf(" %d\n%d\n%d\n", maximum, average, minimum);
            abort();
        }
    }
    if ((c>a)&&(c>b))
    {
        maximum=c;
        if (a<b)
        {
            minimum=a;
            average=b;
            printf(" %d\n%d\n%d\n", maximum, average, minimum);
            abort();
        }
        else
        {
            minimum=b;
            average=a;
            printf(" %d\n%d\n%d\n", maximum, average, minimum);
            abort();
        }
    }
}

int min(int a, int b, int c, int maximum, int average, int minimum)
{
    if ((a<b)&&(a<c))
    {
        minimum=a;
        if (b<c)
        {
            maximum=c;
            average=b;
            printf(" %d\n%d\n%d\n", minimum, average, maximum);
            abort();
        }
        else
        {
            maximum=b;
            average=c;
            printf(" %d\n%d\n%d\n", minimum, average, maximum);
            abort();
        }
    }
    if ((b<a)&&(b<c))
    {
        minimum=b;
        if (a<c)
        {
            maximum=c;
            average=a;
            printf(" %d\n%d\n%d\n", minimum, average, maximum);
            abort();
        }
        else
        {
            maximum=a;
            average=c;
            printf(" %d\n%d\n%d\n", minimum, average, maximum);
            abort();
        }
    }
    if ((c<a)&&(c<b))
    {
        minimum=c;
        if (a<b)
        {
            maximum=b;
            average=a;
            printf(" %d\n%d\n%d\n", minimum, average, maximum);
            abort();
        }
        else
        {
            maximum=a;
            average=b;
            printf(" %d\n%d\n%d\n", minimum, average, maximum);
            abort();
        }
    }
}


double fun_scan() 
{
  double value;
  int result = scanf("%lf", &value);
    if (result != 1)
    {
      printf("error io");
      abort();
    } 
    return value;
}
